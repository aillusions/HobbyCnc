package com.epam.illusions.cnc.inventory.visualization;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

import org.junit.*;

import cnc.emulation.VirtuaMachine;

import static org.junit.Assert.*;

public class MachineViewTest {

	@Test()
	public void onViewVisualisable() throws IOException {
		//new com.epam.illusions.cnc.inventory.visualization.Machine2DView(null).setVisible(true);
		//System.out.println("Press Enter to continue!");
		//new BufferedReader(new InputStreamReader(System.in)).readLine();
	}

	@Test()
	public void onMachineStateChanged() throws IOException {
/*		Machine m = new Machine();
		Machine2DView view2D = new com.epam.illusions.cnc.inventory.visualization.Machine2DView(m);
		view2D.setVisible(true);
		assertTrue("nothing", true);
		
		m.addMachineListener(view2D);
		for(int j = 0; j < 100; j ++)
		{
			m.receiveByte((byte) 12);
			for(int i = 0; i < 50; i ++)
			{
				m.receiveByte((byte) 15);
				m.receiveByte((byte) 5);
				//m.receiveByte((byte) 12);
				//m.receiveByte((byte) 4);
			}
			m.receiveByte((byte) 4);
		}
		System.out.println("Press Enter to continue again!");
		new BufferedReader(new InputStreamReader(System.in)).readLine();*/
	}
}
