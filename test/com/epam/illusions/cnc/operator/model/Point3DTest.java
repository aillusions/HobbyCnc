package com.epam.illusions.cnc.operator.model;

import static org.junit.Assert.*;

import org.junit.Test;

public class Point3DTest {
	@Test()
	public void dummy() {
		assertTrue(true);
	}

	@Test()
	public void ComplexTest() {
		//assertEquals("Equals does not work prperly!",new Point3D(), new Point3D());
		//assertEquals("Equals does not work prperly!",new Point3D(0,0,0), new Point3D());
		//assertEquals("Equals does not work prperly!",new Point3D(1,2,3), new Point3D(1,2,3));
		//assertFalse("Equals does not work prperly!", new Point3D(3,2,1).equals(new Point3D(1,2,3)));
		//assertFalse("Equals does not work prperly!", new Point3D(3,2,1).equals(null));
		//assertEquals("Equals does not work prperly!", "(1,2,3)", new Point3D(1,2,3).toString());
	}
}
